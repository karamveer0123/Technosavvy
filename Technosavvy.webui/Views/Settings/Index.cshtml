@model vmSettings
@{
    ViewData["Title"] = "Optimize Your Experience: Explore TechnoApp   Settings";
}
@section Header {
    <meta name="description" content="Fine-tune your TechnoApp  experience with personalized settings." />
}
@*     @section Styles{
    <link rel="stylesheet" href="~/css/dashboard.css">
    <link rel="stylesheet" href="~/css/style.css">
} *@
<section class="dashboardheader-section bg2">
    <div class="container">
        <!----------------------------->
        <div class="row">
            <div id="market-tabs" class="secuirty-sectdiv33">
                <ul id="settingMenu" class="nav nav-tabs">
                    <li class="nav-item">
                        <a class="nav-link setting1 active"  href="#setting-earn44">  <i class="fa fa-cogs" aria-hidden="true"></i> Settings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link setting2 "   href="#security44"><i class="fa fa-shield" aria-hidden="true"></i> Security</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</section>

<section id="setting-earn44"  class="security-section bg2">
    <div class="container">

        <div class="row">
            <div class="headingcss22"> Setting</div>
        </div>

        <div class="row">
            @await Html.PartialAsync("partial/_ProfileSetting",Model.SettingProfile)


        </div>

    </div>

    <!----------------------------->
</section>
<!---------------------------------------------------------------------->
<section id="security44" class="security-section bg2">
    <div class="container">
        <!----------------------------->
        <div class="row">
            <div class="headingcss22"> Security </div>
        </div>
        <!----------------------------->
        <div id="gauth">
        @await Html.PartialAsync("partial/_GAuth",Model.MultiFactor)
        </div>
        <!----------------------------->
        <!--------------------------------------------------->
        <div class="css55-security-div">
            <div class="css55-heading"> </div>

            <div class="css55-security-box">
                <div class="css55-security-left">
                    <span><i class="fa fa-id-card-o" aria-hidden="true"></i> </span>
                    <span>
                        <div class="css55-security-title">
                            Login Password
                            <p>Login Password is used to login to your account.</p>
                        </div>
                    </span>

                </div>
                <div class="css55-security-center"> <span> </span></div>
                <div class="css55-security-right">
                    <span> <a asp-controller="Settings" asp-action="ChangePassword" class="css55-change-btn">Change </a></span>
                </div>
            </div>



        </div>

       
    </div>
</section>
<!---------------------------------------------------------------------->
@section Scripts{
     @if (ConfigEx.ShouldMini)
    {   
        <script src="~/minjs/CustomScript.min.js"></script>
    }
    else
    {
        <script src="~/js/CustomScript.js"></script>
    }
   
    <script>
        function MultiFactOnSuccess(data)
        {
            alert('');
            $('#gauth').load(data);
        }
    </script>


    <script>

        $(document).ready(function () {
    $(document).on("scroll", onScroll);
    
    //smoothscroll
    $('#settingMenu a[href^="#"]').on('click', function (e) {
        e.preventDefault();
        $(document).off("scroll");
        
        $('#settingMenu a').each(function () {
            $(this).removeClass('active');
        })
        $(this).addClass('active');
      
        var target = this.hash,
            menu = target;
        $target = $(target);
        $('html, body').stop().animate({
            'scrollTop': $target.offset().top+2
        }, 500, 'swing', function () {
            window.location.hash = target;
            $(document).on("scroll", onScroll);
        });
    });
});

function onScroll(event){
    var scrollPos = $(document).scrollTop();
    $('#settingMenu a').each(function () {
        var currLink = $(this);
        var refElement = $(currLink.attr("href"));
        if (refElement.position().top <= scrollPos && refElement.position().top + refElement.height() > scrollPos) {
            $('#settingMenu li a').removeClass("active");
            currLink.addClass("active");
        }
        else{
            currLink.removeClass("active");
        }
    });
}
    </script>
}