function updateThumbnail(e, t) { let r = e.querySelector(".drop-zone__thumb"); if (e.querySelector(".drop-zone__prompt") && e.querySelector(".drop-zone__prompt").remove(), r || (r = document.createElement("div"), r.classList.add("drop-zone__thumb"), e.appendChild(r)), r.dataset.label = t.name, t.type.startsWith("image/")) { const e = new FileReader; e.readAsDataURL(t), e.onload = () => { r.style.backgroundImage = `url('${e.result}')` } } else r.style.backgroundImage = null } document.querySelectorAll(".drop-zone__input").forEach((e => { const t = e.closest(".drop-zone"); t.addEventListener("click", (t => { e.click() })), e.addEventListener("change", (r => { e.files.length && updateThumbnail(t, e.files[0]) })), t.addEventListener("dragover", (e => { e.preventDefault(), t.classList.add("drop-zone--over") })), ["dragleave", "dragend"].forEach((e => { t.addEventListener(e, (e => { t.classList.remove("drop-zone--over") })) })), t.addEventListener("drop", (r => { r.preventDefault(), r.dataTransfer.files.length && (e.files = r.dataTransfer.files, updateThumbnail(t, r.dataTransfer.files[0])), t.classList.remove("drop-zone--over") })) }));